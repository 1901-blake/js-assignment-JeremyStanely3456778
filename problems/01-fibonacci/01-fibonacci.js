/* 1. Fibonacci
Define function: fib(n) 
Return the nth number in the fibonacci sequence. */
function fib(n) {
  
  let varOne = 1, VarTwo = 0, temporaryVar;

  while (n >= 0){
    temporaryVar = varOne;
    varOne = varOne + VarTwo;
    VarTwo = temporaryVar;
    num--;
  }

  return b;

}
